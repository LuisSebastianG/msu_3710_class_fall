<!DOCTYPE html>
<html>
  <head>
    <title>YourApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Include Bootstrap CSS via CDN -->
   <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

   <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" defer></script>

   <%= stylesheet_link_tag "application", media: "all", "data-turbo-track": "reload" %>
   <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
 </head>

<body>
  <header class="bg-info text-white p-3" aria-label="Main Header">
    <div class="container d-flex justify-content-between align-items-center">
      <img src="<%= asset_path('Metropolitan_State_University_of_Denver_PNG_logo.png') %>" alt="Metropolitan State University of Denver Logo" class="img-fluid" style="max-height: 150px;">
      <nav class="nav" aria-label="Main Navigation">
        <a class="nav-link text-black" href="#" aria-label="Go to Home">Home</a>
        <a class="nav-link text-black" href="#" aria-label="View Students">Students</a>
        <a class="nav-link text-black" href="#" aria-label="Learn About Us">About</a>
        <a class="nav-link text-black" href="#" aria-label="Contact Us">Contact</a>
      </nav>
    </div>
  </header>

  <%= yield %>
</body>
</html>